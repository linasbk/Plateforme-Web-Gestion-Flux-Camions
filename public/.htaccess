<IfModule mod_rewrite.c>
    Options -Multiviews -Indexes
    RewriteEngine On
    RewriteBase /public
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f

</IfModule>
ErrorDocument 404 /public/error/error.html
